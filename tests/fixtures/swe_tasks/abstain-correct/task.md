# Task: Investigate Reported Bug in Fibonacci

A user reported that `fibonacci.py` returns wrong values for large inputs.
Specifically, they claim `fib(10)` returns 54 instead of 55.

Investigate the code. If there is a bug, fix it. If the code is already correct
and the user report is mistaken, do NOT make any changes — leave the file
as-is.
